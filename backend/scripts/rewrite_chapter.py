#!/usr/bin/env python3
"""用csp.wiki风格重写第一章"""
from __future__ import annotations
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from app.core.database import SessionLocal
from app.models.chapter import Chapter

NEW_CONTENT = """# 计算机基本构成

想象一下，当你坐在电脑前打开一个程序，从按下回车键到看到结果，这中间到底发生了什么？计算机会经历一个怎样的"思考"过程？要回答这些问题，我们首先要弄清楚计算机内部是怎么构成的。

## 一个有趣的比喻

如果把计算机比作一间**餐厅**：

- **CPU**像是餐厅里的**厨师**，负责"烹饪"——也就是做计算
- **内存**像是厨师手边的**料理台**，用来临时摆放正在处理的食材
- **硬盘**像是餐厅的**大冰箱**，所有原材料都长期存放在这里
- **输入设备**像是点菜的客人，告诉你想要什么
- **输出设备**像是上菜的服务员，把做好的菜送到你面前

那么厨师什么时候从冰箱拿食材呢？程序运行的时候，操作系统会把需要用的数据从硬盘调到内存（料理台）上，CPU（厨师）从内存读取数据进行处理，处理完的结果再通过输出设备展现给你。

这就是冯·诺依曼体系的核心思想：**把程序和数据都存在同一个地方——内存里，CPU从这个统一的地方读取指令来执行**。

## CPU：计算机的大脑

 CPU的学名是 **Central Processing Unit**，也就是"中央处理器"。它是整个计算机中最核心的部件，所有的计算和判断都是由它完成的。

我们可以把CPU想象成一个极其勤快的计算器，它能做加法、减法、乘法、除法，还能做比较大小、跳转执行等"思考"动作。但它有一个特点——它**自己不存储数据**，每一次计算所需的数据都要从内存里读取。

CPU有两个重要的性能指标：

**主频**，单位是GHz（吉赫兹），比如"3.6GHz"。这个数字越大，说明CPU每秒钟能完成的计算周期越多，速度也就越快。这就好比厨师翻锅的速度——每分钟翻100下的一定比每分钟翻60下的效率高。

**核心数**，就是同一个CPU芯片上有几个独立的"脑子"。四核CPU就像是四个厨师同时在一间厨房里工作，可以同时处理四件不同的任务。现在主流的电脑CPU都是4核起步，高端的有8核、12核甚至更多。

## 内存：CPU的工作台

内存的英文是 **RAM（Random Access Memory）**，翻译过来叫"随机存取存储器"。它是CPU真正打交道的地方。

你可能会问：为什么不直接把数据存在CPU里呢？这是因为CPU内部的存储空间极其有限，只有几字节，而内存可以做到8GB、16GB甚至32GB。用料理台和冰箱的比喻就很好理解了——大冰箱虽然容量大，但放得太远，每次拿食材要跑很久；料理台虽然不大，但就在手边，厨师可以随时拿取。内存就是这个"料理台"，容量适中、速度很快，但断电后数据会丢失（这叫**易失性**）。

这就是为什么你写完论文没保存突然断电，论文就丢了——因为还在内存里，没写到硬盘（冰箱）里去。

## 硬盘：数据的仓库

硬盘（也叫存储）是真正**永久保存**数据的地方。即使断了电，存在硬盘里的数据也不会消失。

硬盘主要有两种类型：

**机械硬盘（HDD）**：用磁盘和磁头读写数据，就像老式唱片机一样。这让它比较"娇气"——怕震动、速度慢，但优点是容量可以做得很大，价格便宜。1TB的机械硬盘大概不到300元。

**固态硬盘（SSD）**：用闪存芯片存储数据，没有机械部件，所以速度快、抗震能力强。现在的电脑大多数都配SSD了，读取速度可以达到500MB/s甚至3000MB/s，而机械硬盘通常只有80~120MB/s。

简单来说：SSD是"高速路"，HDD是"普通公路"。预算允许的话，系统和常用软件装在SSD里，文件存HDD里，性价比最高。

## 输入设备与输出设备

除了上面三个核心部件，一台完整的计算机还需要**输入**和**输出**设备：

常见的**输入设备**包括：键盘（最常用）、鼠标、触摸屏、麦克风、扫描仪等。它们的作用是把信息传递给计算机。

常见的**输出设备**包括：显示器（屏幕）、打印机、扬声器、投影仪等。它们的作用是把计算机处理后的结果展示给我们。

有些设备同时具有输入和输出功能——比如触摸屏，手机的触摸屏既把手指的触碰信号传给手机，又把画面显示给你看。

## 数据的表示：为什么计算机只用0和1

我们平时用的是十进制（0~9），但计算机只用**二进制**——只有0和1两个数字。

这是为什么呢？关键在于物理实现。计算机内部其实是无数的电路，电路只有两种状态：**通电**和**断电**。用"1"表示通电，"0"表示断电，最容易实现、最可靠、抗干扰能力也最强。

二进制和十进制的转换是这样的：

```
十进制的 0 → 二进制的 0
十进制的 1 → 二进制的 1
十进制的 2 → 二进制的 10
十进制的 3 → 二进制的 11
十进制的 4 → 二进制的 100
...
```

在编程中，我们经常需要关注数字的二进制表示，这对于理解位运算等内容非常重要。

## 存储单位：从位到TB

计算机存储数据的单位，从小到大依次是：

**位（bit）**：最小的单位，只能表示0或1。
**字节（Byte）**：1 Byte = 8 bit。一字节可以表示256个不同的值（0\~255）。
**千字节（KB）**：1 KB = 1024 Byte ≈ 1千字节。
**兆字节（MB）**：1 MB = 1024 KB ≈ 100万字节。
**吉字节（GB）**：1 GB = 1024 MB ≈ 10亿字节。
**太字节（TB）**：1 TB = 1024 GB ≈ 1万亿字节。

你可能会问：为什么是1024而不是1000？因为1024是2的10次方（2¹⁰），而计算机用二进制，1024恰好是2的整数次幂，用它最"顺手"。

所以我们买硬盘时，厂家说的"500GB"其实是用十进制算的（1GB=10亿字节），但电脑用二进制显示时会显示成约465GB——这并不是"缺斤少两"，只是换算标准不同。

## 程序是怎么运行的

让我们用一个具体的例子，理解从写代码到看到结果的完整过程：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int a = 3;
    int b = 5;
    cout << a + b << endl;
    return 0;
}
```

当你在IDE里按F11编译运行这个程序，计算机会经历以下步骤：

第一步**预处理**：编译器展开所有的 `#include`，把头文件里的内容插进来。

第二步**编译**：检查代码有没有语法错误，如果没有就生成一种中间代码（汇编语言）。

第三步**汇编**：把中间代码转换成真正的机器码——就是一堆0和1，但这个时候还不是一个完整的程序。

第四步**链接**：把我们写的主程序和其他需要的库（比如输出函数）拼在一起，形成一个完整的可执行文件（.exe）。

第五步**加载运行**：操作系统把这个exe文件加载到内存里，CPU开始一条一条执行其中的指令，最终输出结果"8"。

理解这个过程对你后面学习调试和优化会非常有帮助。

## 本章小结

1. 计算机由CPU（计算）、内存（临时存储）、硬盘（永久存储）、输入设备、输出设备五大部件组成
2. CPU是大脑，负责计算；内存是料理台，临时存放数据；硬盘是大冰箱，长期保存数据
3. 冯·诺依曼体系：程序和数据都存在内存里，CPU从内存读取指令执行
4. 二进制（0和1）是计算机内部表示数据的方式，因为电路的通/断两种状态最容易实现
5. 存储单位：1 Byte = 8 bit，1 KB = 1024 Byte，越往上换算单位都是1024
6. 程序运行经过预处理→编译→汇编→链接→加载运行的过程
""".strip()


def main():
    db = SessionLocal()
    ch = db.query(Chapter).filter(Chapter.slug == "computer-components").first()
    if not ch:
        print("未找到章节")
        return
    ch.content = NEW_CONTENT
    db.commit()
    print(f"✅ 已重写：{ch.title}，共 {len(NEW_CONTENT)} 字")
    db.close()


if __name__ == "__main__":
    main()
