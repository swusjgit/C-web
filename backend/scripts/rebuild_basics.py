#!/usr/bin/env python3
"""用csp.wiki风格+PDF图片，重建基础知识与编程环境12章"""
from __future__ import annotations
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from app.core.database import SessionLocal
from app.models.chapter import Chapter

MEDIA = "/media/chapters/basics"

# 每章内容 = Markdown文字 + PDF图片引用
# PDF页面图片 → /media/chapters/basics/embed_p{页码}.png

CHAPTERS = {

    "computer-components": """# 计算机基本构成

想象一下，当你坐在电脑前打开一个程序，从按下回车键到看到结果，这中间到底发生了什么？计算机会经历一个怎样的"思考"过程？让我们从最核心的问题开始：**计算机里面，到底有什么？**

![计算机基本构成——五大部件](/media/chapters/basics/embed_p03.png)

## 一个有趣的比喻

如果把计算机比作一间**餐厅**：

- **CPU**像是餐厅里的**厨师**，负责"烹饪"——也就是做计算
- **内存**像是厨师手边的**料理台**，用来临时摆放正在处理的食材
- **硬盘**像是餐厅的**大冰箱**，所有原材料都长期存放在这里
- **输入设备**像是点菜的客人，告诉你想要什么
- **输出设备**像是上菜的服务员，把做好的菜送到你面前

那么厨师什么时候从冰箱拿食材呢？程序运行的时候，操作系统会把需要用的数据从硬盘调到内存（料理台）上，CPU（厨师）从内存读取数据进行处理，处理完的结果再通过输出设备展现给你。

## CPU：计算机的大脑

CPU的学名是 **Central Processing Unit**，也就是"中央处理器"。它是整个计算机中最核心的部件，所有的计算和判断都是由它完成的。

![CPU内部结构示意图——控制单元、运算单元ALU、存储单元MMU](/media/chapters/basics/embed_p02.png)

我们可以把CPU想象成一个极其勤快的计算器。CPU有两个重要的性能指标：

**主频**，单位是GHz，比如"3.6GHz"。这个数字越大，说明CPU每秒钟能完成的计算周期越多，速度也就越快。

**核心数**，就是同一个CPU芯片上有几个独立的"脑子"。四核CPU就像是四个厨师同时在一间厨房里工作。

## 内存：CPU的工作台

内存的英文是 **RAM（Random Access Memory）**，叫"随机存取存储器"。它是CPU真正打交道的地方。

你可以这样理解——大冰箱（硬盘）容量大但放得太远，每次拿食材要跑很久；料理台（内存）虽然不大，但就在手边，厨师可以随时拿取。内存就是这个"料理台"，但断电后数据会丢失（这叫**易失性**）。

## 硬盘：数据的仓库

硬盘是真正**永久保存**数据的地方。即使断了电，硬盘里的数据也不会消失。

主要两种类型：**固态硬盘（SSD）**速度快、体积小；**机械硬盘（HDD）**容量大、价格便宜。预算允许的话，系统和常用软件装在SSD里，性价比最高。

## I/O设备：人机交互的桥梁

除了CPU、内存、硬盘三个核心部件，还需要**输入**和**输出**设备：

- **输入设备**：键盘、鼠标、触摸屏、麦克风——把信息传递给计算机
- **输出设备**：显示器、打印机、扬声器——把计算机处理后的结果展示给我们

![CPU、内存、硬盘关系图——数据在三者之间的流动](/media/chapters/basics/embed_p04.png)

## 本章小结

1. 计算机由CPU（计算）、内存（临时存储）、硬盘（永久存储）、输入设备、输出设备五大部件组成
2. CPU是大脑，负责计算；内存是料理台，临时存放数据；硬盘是大冰箱，长期保存数据
3. 冯·诺依曼体系：程序和数据都存在内存里，CPU从内存读取指令执行
4. 存储单位：1 Byte = 8 bit，1 KB = 1024 Byte，越往上换算单位都是1024
""",

    "os-basics": """# Windows与Linux基本操作

操作系统（OS）是管理计算机硬件和软件资源的系统软件，是用户与计算机之间的桥梁。打个比方：如果把计算机比作一家餐厅，那么操作系统就是餐厅经理——负责协调厨师（CPU）、仓库（硬盘）、采购（输入设备）等各个部门，让整个系统高效运转。

![Linux Ubuntu桌面——终端和文件管理器同时运行](/media/chapters/basics/embed_p05.png)

## Windows基本操作

### 常用快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + C` | 复制 |
| `Ctrl + V` | 粘贴 |
| `Ctrl + X` | 剪切 |
| `Ctrl + Z` | 撤销 |
| `Ctrl + S` | 保存 |
| `Alt + Tab` | 切换窗口 |
| `Win + E` | 打开文件资源管理器 |
| `Ctrl + Shift + Esc` | 打开任务管理器 |

### Windows命令行（CMD）

打开方式：Win+R → 输入`cmd` → 回车

```bash
# 切换目录
cd Desktop             # 进入桌面
cd ..                 # 返回上级目录

# 查看当前目录内容
dir

# 创建文件夹
mkdir myproject

# ping测试网络
ping www.luogu.com
```

## Linux基本概念

Linux是一种开源操作系统内核，基于Unix设计，广泛应用于服务器（90%以上）和竞赛环境。

### 目录结构

```
/                   # 根目录
├── home/          # 用户主目录
├── usr/           # 系统程序
├── etc/           # 配置文件
├── var/           # 日志等变量文件
└── root/          # 超级用户主目录
```

### 常用命令

```bash
ls -la              # 列出所有文件（含隐藏文件）
cd /home/user       # 切换到指定目录
pwd                 # 显示当前路径
mkdir project       # 创建文件夹
rm -rf project     # 强制删除文件夹
cp a.txt b.txt     # 复制文件
```

## Linux下编译运行C++

在Linux终端中，我们可以直接用 `g++` 编译器编译C++程序：

```bash
# 编写代码
vim HelloWorld.cpp

# 简单编译（生成 a.out）
g++ HelloWorld.cpp
./a.out

# 指定输出文件名
g++ HelloWorld.cpp -o HelloWorld
./HelloWorld
```

![Linux终端运行截图——g++编译并运行HelloWorld程序](/media/chapters/basics/embed_p05.png)

## 竞赛环境选择

| 场景 | 推荐系统 | 说明 |
|------|---------|------|
| CSP-J初赛 | Windows | 笔试为主 |
| CSP-J复赛 | Windows（Dev-C++） | 国内主流 |
| 进阶训练 | Linux（Ubuntu） | 更接近竞赛环境 |

## 本章小结

1. 操作系统是用户与计算机硬件之间的桥梁
2. Windows：图形界面+CMD命令行，常用Ctrl+C/V快捷键
3. Linux：命令行为主，必备命令：ls、cd、mkdir、rm、cp、mv
4. 竞赛推荐Windows+Dev-C++环境，Linux可用g++命令行编译
""",

    "network-basics": """# 计算机网络与Internet

当你打开浏览器，输入 `www.luogu.com` 并按下回车，一眨眼的功夫，网页就出现在屏幕上了——这中间究竟发生了什么？答案藏在计算机网络的世界里。

## IP地址：网络中的门牌号

 IP地址是计算机在网络中的唯一标识，就像每家每户的地址一样：

```
IPv4地址示例：192.168.1.100
IPv6地址示例：2001:0db8:85a3:0000:0000:8a2e:0370:7334
```

IPv4地址由4段数字组成，每段0~255，共约42亿个地址。IPv6是新一代协议，地址近乎无限。

## 域名与DNS

域名是IP地址的易记别名：

| 域名 | 实际用途 |
|------|---------|
| www.luogu.com | 洛谷（信息学竞赛） |
| oi-wiki.org | OI Wiki知识百科 |
| www.baidu.com | 百度搜索 |

**DNS**（域名系统）就像一本电话簿，负责将域名转换为IP地址。你输入 `luogu.com`，DNS帮你找到对应的服务器IP。

## 端口号

一台计算机上运行多个网络服务，端口号用于区分不同的服务：

| 端口 | 服务 |
|------|------|
| 80 | HTTP网页 |
| 443 | HTTPS（加密网页） |
| 3306 | MySQL数据库 |
| 8000 | 本地Web服务常用端口 |

## HTTP与HTTPS

- **HTTP**：超文本传输协议，用于浏览器访问网页
- **HTTPS**：HTTP的安全版本，数据加密传输

## 竞赛学习常用网站

### 洛谷（luogu.com）

**国内最权威的信息学竞赛学习平台**

| 功能 | 说明 |
|------|------|
| 在线评测 | 提交代码，机器评测 |
| 题库 | 涵盖CSP-J/S各级题目 |
| 题解 | 众多选手分享的解题思路 |

### OI Wiki（oi-wiki.org）

**信息学竞赛知识百科**——系统梳理算法和数据结构知识，配有代码示例，完全免费开源。

### Codeforces

**国际顶级竞赛平台**——每周1~2场在线比赛，题目质量高，适合进阶训练。

### AtCoder

**日本竞赛平台**——ABC（AtCoder Beginner Contest）适合入门，体系完整。

## 本章小结

1. IP地址是计算机在网络中的唯一标识
2. DNS将域名解析为IP地址
3. HTTP/HTTPS是网页传输协议
4. 洛谷是国内CSP-J/S学习首选平台
5. OI Wiki是算法知识百科
""",

    "computer-history": """# 计算机历史与用途

计算机是如何诞生的？它经历了怎样的发展？信息学竞赛在这个大舞台上又扮演什么角色？让我们从历史中寻找答案。

## 计算机的诞生

1946年，世界上第一台电子计算机 **ENIAC** 在美国宾夕法尼亚大学诞生。这台计算机占地170平方米，重达30吨，使用了18000个电子管，但每秒只能进行5000次加法运算——如今随便一部手机的处理器都能轻松超越它。

1946年，冯·诺依曼提出了**冯·诺依曼体系**，成为现代计算机的理论基础：

> 程序和数据都存储在内存中，CPU从内存读取指令执行。

## 计算机发展四代

| 时代 | 时间 | 代表技术 |
|------|------|---------|
| 第一代 | 1940s-1950s | 电子管 |
| 第二代 | 1960s | 晶体管 |
| 第三代 | 1970s | 集成电路 |
| 第四代 | 1980s-今 | 大规模集成电路 |

1980年代，个人计算机开始普及；2000年代，笔记本电脑走入千家万户；2010年代，移动互联网爆发；2020年代，AI人工智能成为新的浪潮。

## 信息学竞赛体系

**信息学竞赛**是利用计算机解决算法问题的比赛，从小到大分为：

```
CSP-J/S → NOIP → 省队选拔 → NOI → IOI
（入门）  （省级）   （国家）  （国家）  （国际）
```

| 竞赛 | 说明 |
|------|------|
| CSP-J | 非专业级软件能力认证-入门级 |
| CSP-S | 非专业级软件能力认证-提高级 |
| NOIP | 全国青少年信息学奥林匹克联赛（省级） |
| NOI | 全国青少年信息学奥林匹克（国家） |
| IOI | 国际信息学奥林匹克（全球） |

![全国青少年信息学奥林匹克系列竞赛大纲（2023年修订版）封面](/media/chapters/basics/embed_p03.png)

## 为什么学信息学？

1. **锻炼逻辑思维能力**——解决问题的核心能力
2. **助力升学**——CSP-J/S获奖可助力升学
3. **培养耐心**——调试程序的过程培养耐心和细心
4. **未来竞争力**——编程能力是未来工作的加分项
5. **很有趣**——做出成果的成就感很强

## 本章小结

1. 计算机从1946年ENIAC发展到今天，经历了四代
2. 冯·诺依曼体系是现代计算机的理论基础
3. 信息学竞赛体系：CSP-J/S → NOIP → NOI → IOI
4. 学习编程不仅是为了竞赛，更能培养解决问题的能力
""",

    "noi-history": """# NOI及相关活动历史

信息学竞赛在中国是如何发展起来的？有哪些重要的比赛？它们之间是什么关系？让我们一起了解这个丰富的竞赛生态。

## CCF简介

**CCF**（中国计算机学会，China Computer Federation）成立于1962年，是国内计算机领域最具影响力的学术组织之一，负责主办NOI系列所有竞赛。

![中国计算机学会(CCF)官方标志](/media/chapters/basics/embed_p03.png)

## 竞赛体系详解

### 入门级：CSP-J

CSP-J/S（Certified Software Professional）是NOIP取消后CCF开设的非专业级软件能力认证，分为入门级（J）和提高级（S）。

- 第一轮（9月）：笔试，考察计算机理论和基本算法
- 第二轮（10月）：上机编程，4道题

### 进阶层：NOIP

NOIP（全国青少年信息学奥林匹克联赛）是省级联赛，每年11~12月举行，4小时4道题。参赛者需要在CSP-S第二轮中取得一定名次才能获得参赛资格。

### 国家决赛：NOI

NOI（全国青少年信息学奥林匹克）是国内最高级别的中学生信息学竞赛，每年7月举行，各省派代表队参加。前50名选手组成国家集训队，获得保送资格。

### 国际舞台：IOI

IOI（国际信息学奥林匹克）是全球最高水平的信息学竞赛，每年夏天举行，每个国家有4名选手参赛。

## 著名中国选手

中国在国际赛场上一直表现优异：

| 年份 | 选手 | 获奖 |
|------|------|------|
| 2024 | 虞皓翔（浙江） | IOI金牌 |
| 2023 | 许庭强（浙江） | IOI金牌 |
| 2022 | 刘梓非（江苏） | IOI金牌 |

这些选手的共同特点是：数学基础扎实、大量刷题训练、善于总结归纳、心理素质好。

## 竞赛时间线

```
9月  CSP-J/S第一轮（笔试）
10月 CSP-J/S第二轮（上机）
11月 NOIP（省级联赛）
次年1-4月 省队选拔赛
7月  NOI（国家决赛）
7-8月 IOI（国际决赛）
```

## 本章小结

1. CCF（中国计算机学会）主办NOI系列竞赛
2. 竞赛体系：CSP-J/S → NOIP → NOI → IOI
3. CSP-J/S是第一轮门槛，NOIP是省级联赛，NOI是国家决赛
4. 科学的备赛规划比盲目刷题更重要
""",

    "noi-rules": """# NOI及相关活动规则

了解了历史之后，我们需要知道竞赛是怎么进行的。CSP-J/S、NOIP、NOI的赛制有何不同？评分标准是什么？有哪些应试技巧？

## CSP-J/S比赛规则

### 比赛形式

| 项目 | CSP-J（入门级） | CSP-S（提高级） |
|------|----------------|----------------|
| 第一轮（初赛） | 笔试，2.5小时 | 笔试，2.5小时 |
| 第二轮（复赛） | 上机编程，3.5小时 | 上机编程，4小时 |

### 初赛（笔试）

- **题型**：选择题 + 阅读程序题 + 完善程序题
- **内容**：计算机基础知识、算法常识、程序理解
- **晋级**：按成绩排名，比例由CCF统一规定

### 复赛（上机编程）

- **语言**：C++（推荐）、C、Pascal
- **题目数量**：4道题（从易到难）
- **评测方式**：机器评测，10~20个测试点/题
- **评分**：按测试点给分，部分分解法也能得分

## 评分标准

CSP-J/S采用**IOI制**，每题有多个测试点，按通过比例给分：

| 方式 | 说明 |
|------|------|
| **ACM制** | 每题通过得1分，按做题数量排名 |
| **IOI制** | 每题多个测试点，按通过比例给分 |

竞赛中常见结果代码：

| 结果 | 含义 |
|------|------|
| AC | ✅ 正确，通过 |
| WA | ❌ 答案错误 |
| TLE | ⏱️ 超时 |
| MLE | 💾 超内存 |
| RE | 💥 运行时错误 |
| CE | 🔴 编译错误 |

## 竞赛应试技巧

### 时间分配策略

```
4小时 = 240分钟
├── 读题 + 确定顺序：10分钟
├── 第一题（简单）：30~40分钟
└── 后三题：按性价比分配
```

**重要提示：不要死磕一道题！**

### 竞赛代码规范

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // 读入
    int n; cin >> n;

    // 核心算法

    return 0;
}
```

## 本章小结

1. CSP-J/S分初赛（笔试）和复赛（上机编程）
2. 复赛采用机器评测，按测试点给分
3. 竞赛策略：先读所有题，确定性价比最高的先做
4. 注意文件读入输出格式！复赛必须读写文件
""",

    "bit-byte-word": """# 位、字节与字

我们知道计算机用二进制（0和1）来表示一切数据。但为什么是二进制？位、字节、字之间是什么关系？这一章我们来深入理解这些底层概念。

## 为什么是二进制？

计算机内部其实是无数的电路，电路只有两种状态：**通电**（表示1）和**断电**（表示0）。用二进制最容易实现、最可靠、抗干扰能力最强。

二进制只有0和1两个数字：

```
十进制的 0 → 二进制的 0
十进制的 1 → 二进制的 1
十进制的 2 → 二进制的 10
十进制的 3 → 二进制的 11
十进制的 4 → 二进制的 100
...
```

## 位（Bit）与字节（Byte）

- **位（bit）**：最小的数据单位，只能表示0或1
- **字节（Byte）**：存储的基本单位，**1 Byte = 8 bit**

一字节可以表示256个不同的值（0~255）。

| 存储内容 | 占用字节数 | 示例 |
|---------|-----------|------|
| 英文字符 | 1字节 | 'A' = 65 |
| 汉字 | 2~4字节 | "你好" UTF-8编码占6字节 |
| 整数（int） | 4字节 | ~±21亿 |
| 长整数（long long） | 8字节 | ~±9×10¹⁸ |

## 存储单位换算

```
1 KB = 1024 Byte  = 2¹⁰ Byte
1 MB = 1024 KB    = 2²⁰ Byte  ≈ 100万字节
1 GB = 1024 MB    = 2³⁰ Byte  ≈ 10亿字节
```

**为什么是1024而不是1000？** 因为1024 = 2¹⁰，是2的整数次幂，计算机用起来最"顺手"。

## 字（Word）

字是CPU一次处理的数据单位：

| 系统类型 | 字长 | 说明 |
|---------|------|------|
| 32位系统 | 4字节 | CPU一次处理32位数据 |
| 64位系统 | 8字节 | CPU一次处理64位数据 |

## 位运算

位运算是对二进制数的每一位进行操作的运算，是竞赛中的重要技巧：

| 运算符 | 名称 | 说明 |
|--------|------|------|
| `&` | 按位与 | 两位都为1时结果为1 |
| `\|` | 按位或 | 两位有一个为1时结果为1 |
| `^` | 按位异或 | 两位相异时结果为1 |
| `~` | 按位取反 | 0变1，1变0 |
| `<<` | 左移 | 二进制位全部左移，低位补0 |
| `>>` | 右移 | 二进制位全部右移，高位补0或符号位 |

![位运算符汇总表——六种位运算的规则详解](/media/chapters/basics/embed_p08.png)

## 本章小结

1. **位（bit）**：最小单位，0或1
2. **字节（Byte）**：存储基本单位，1 Byte = 8 bit
3. **字（Word）**：CPU一次处理的数据单位，32位/64位系统
4. 存储单位换算：1 KB = 1024 Byte，越往上换算单位都是1024
5. 位运算：&、|、^、~、<<、>> 是竞赛中的重要技巧
""",

    "pl-basics": """# 程序设计语言基础

我们写的代码，计算机是如何执行的呢？这就涉及到编程语言的两种执行方式：**编译**和**解释**。理解这个区别，对学习C++至关重要。

![编译型语言与解释型语言的执行流程对比](/media/chapters/basics/embed_p06.png)

## 编译型语言 vs 解释型语言

### 编译型语言（以C/C++为例）

**定义**：源代码通过**编译器**一次性转换为目标平台的机器码。转换完成后，程序可以直接在目标机器上运行，不再依赖编译器。

**核心流程**：

```
源代码.cpp
    │
    ▼ 预处理（展开头文件）
预处理后的代码
    │
    ▼ 编译（检查语法，生成汇编）
汇编代码
    │
    ▼ 汇编（转成机器码）
目标文件.o
    │
    ▼ 链接（合并库函数）
可执行文件.exe
```

### 解释型语言（以Python为例）

**定义**：源代码通过**解释器**逐行读取、逐行转换为机器码并执行。运行时必须依赖解释器环境，不生成独立可执行文件。

### 对比总结

| 特性 | 编译型（C++） | 解释型（Python） |
|------|-------------|----------------|
| 运行速度 | 快（机器码直接运行） | 较慢（逐行解释） |
| 是否需要编译器 | 是 | 否（需要解释器） |
| 是否生成exe | 是 | 否 |
| 跨平台 | 需重新编译 | 直接可运行（需解释器） |

**C++是编译型语言，运行速度快，是信息学竞赛的首选语言。**

## C++标准

| 标准 | 年份 | 特性 |
|------|------|------|
| C++98 | 1998 | 第一个ISO标准 |
| C++11 | 2011 | 重大更新：auto、范围for、lambda |
| **C++17** | 2017 | 结构化绑定、if constexpr |
| **C++20** | 2020 | concepts、ranges、协程 |

**竞赛推荐使用C++17标准。**

## 第一个C++程序

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cout << "Hello, World!" << endl;
    return 0;
}
```

## 本章小结

1. C++是**编译型语言**，运行速度快，是竞赛首选
2. 编译过程：预处理→编译→汇编→链接→可执行文件
3. Python等是解释型语言，逐行执行，速度较慢
4. 推荐使用C++17标准
5. `ios::sync_with_stdio(false)` 加速输入输出
""",

    "gui-file-ops": """# 图形界面与文件操作

程序写好了怎么保存？程序运行时怎么读取数据？文件操作是编程中非常重要的一环。这一章我们从图形界面和文件操作两个维度来学习。

## Windows图形界面操作

### 资源管理器

打开资源管理器：`Win + E`

常用操作：

| 操作 | 鼠标方式 | 快捷键 |
|------|---------|--------|
| 新建文件夹 | 右键 → 新建 → 文件夹 | - |
| 重命名 | 右键 → 重命名 或 F2 | - |
| 全选 | - | Ctrl+A |
| 保存 | - | Ctrl+S |

### 文件路径

**绝对路径**：从盘符开始的完整路径
```
C:\\Users\\Admin\\Desktop\\test.cpp
```

**相对路径**：从当前目录出发的路径
```
.          # 当前目录
..         # 上级目录
./src      # 当前目录下的src文件夹
```

## C++文件操作（竞赛重点）

### 方法一：freopen（竞赛最常用）

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    freopen("input.txt", "r", stdin);   // 从input.txt读入
    freopen("output.txt", "w", stdout);  // 输出到output.txt

    int a, b;
    cin >> a >> b;             // 实际从input.txt读
    cout << a + b << endl;     // 实际输出到output.txt

    return 0;
}
```

### 方法二：fstream（更通用）

```cpp
#include <fstream>
using namespace std;

int main() {
    ifstream fin("input.txt");    // 打开输入文件
    ofstream fout("output.txt");   // 创建输出文件

    int n; fin >> n;
    fout << n * 2 << endl;

    fin.close();
    fout.close();
    return 0;
}
```

### 方法三：命令行参数（进阶用法）

```cpp
int main(int argc, char* argv[]) {
    if (argc >= 2) freopen(argv[1], "r", stdin);
    if (argc >= 3) freopen(argv[2], "w", stdout);
    // ...
}
```

## 竞赛注意事项

1. **复赛必须用文件读入输出！** 不要只依赖cin/cout
2. 本地测试用文件，提交代码时也要用freopen
3. Windows路径分隔符用`\\`或直接用`/`

## 本章小结

1. Windows快捷键：Ctrl+C/V复制粘贴，F2重命名，Win+E打开资源管理器
2. 相对路径`.`当前目录，`..`上级目录
3. 竞赛最常用`freopen`读写文件
4. 复赛必须读写文件，不要只依赖cin/cout
""",

    "dev-cpp": """# Dev-C++使用

Dev-C++是Windows下最适合CSP-J竞赛的集成开发环境（IDE）。免费、轻量、界面简洁，国内竞赛环境普遍支持。这一章我们详细学习它的使用方法。

## 下载与安装

1. 官网下载：https://www.bloodshed.net/
2. 运行安装程序，一路下一步
3. 安装时选择**中文语言**和**GNU GCC编译器**

## 基本操作

### 新建和保存

1. **新建源文件**：文件 → 新建 → 源代码（Ctrl+N）
2. **保存**：`Ctrl + S`（重要！先保存再编译）
3. **另存为**：`Ctrl + Shift + S`

### 编译运行

| 按键 | 功能 |
|------|------|
| **F11** | 编译运行（最常用！） |
| F9 | 仅编译 |
| F10 | 仅运行（不重新编译） |
| Ctrl+F11 | 编译并以管理员身份运行 |

按F11后，计算机会自动编译代码，若无错误则直接运行，输出结果会显示在黑色控制台窗口中。

## 调试功能

### 设置断点

在代码行号左侧单击，会出现红色圆点。程序运行到断点处会自动暂停，方便检查变量值。

### 单步执行

| 按键 | 功能 |
|------|------|
| F7 | 单步执行（进入函数） |
| F8 | 单步执行（跳过函数） |
| Shift+F7 | 执行到下一个断点 |

### 防止闪退

程序运行完就关闭，看不到输出？两个解决方法：

```cpp
// 方法一：在return前加暂停（本地测试用）
system("pause");

// 方法二（推荐）：用Ctrl+F5运行（"执行不调试"）
```

## 竞赛代码模板

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // ===== 在此下方开始写代码 =====

    int n;
    cin >> n;
    cout << n << endl;

    // ===== 在此上方结束代码 =====

    return 0;
}
```

## Dev-C++设置优化

建议开启的编译选项：**工具 → 编译选项 → 编译时加入以下命令**：

```
-static -s
```

这样生成的可执行文件不依赖外部DLL，更容易在不同电脑运行。

## 本章小结

1. Dev-C++是Windows下最适合竞赛入门的IDE
2. **F11**是最高频快捷键：编译运行
3. 调试用F7单步执行，F8跳过函数
4. 养成好习惯：先保存（Ctrl+S）再编译运行（F11）
""",

    "codeblocks": """# Code::Blocks使用

Code::Blocks是另一个流行的跨平台C++ IDE，支持Windows、Linux、macOS。它的调试功能比Dev-C++更强大，适合需要深入调试的训练场景。

## 下载与安装

- 官网：https://www.codeblocks.org/
- 下载版本：选择带mingw的安装包（约100MB）

## 基本操作

### 新建项目

1. **File → New → Project**
2. 选择 **Console Application**（控制台程序）
3. 选择 **C++**
4. 输入项目名称和路径
5. 选择编译器（GNU GCC Compiler）
6. 点击 **Finish**

### 编译运行

| 按键 | 功能 |
|------|------|
| **F9** | 编译并运行 |
| Ctrl+F9 | 仅编译 |
| Ctrl+F10 | 仅运行 |
| F8 | 开始调试 |

### 多文件项目管理

```cpp
// main.cpp
#include <bits/stdc++.h>
using namespace std;

int maxValue(int a, int b);  // 函数声明

int main() {
    cout << maxValue(3, 7) << endl;
    return 0;
}

// func.cpp
int maxValue(int a, int b) {  // 函数定义
    return (a > b) ? a : b;
}
```

在Code::Blocks中：新建Empty file → 保存为`func.cpp` → 右键项目 → Add Files。

## 调试功能

### 设置断点

在代码行号左侧单击，出现蓝色圆点表示断点设置成功。

### 调试面板

| 面板 | 内容 |
|------|------|
| Watches | 监视变量值（最重要！） |
| Call stack | 函数调用栈 |

### 调试快捷键

| 按键 | 功能 |
|------|------|
| F8 | 开始调试 |
| F4 | 运行到光标 |
| F7 | 单步执行（进入函数） |
| Shift+F7 | 单步执行（跳过函数） |

## Code::Blocks vs Dev-C++

| 特性 | Code::Blocks | Dev-C++ |
|------|--------------|---------|
| 平台支持 | Windows/Linux/macOS | 主要Windows |
| 调试功能 | 更强大 | 较基础 |
| 体积 | ~100MB | ~50MB |
| 竞赛推荐度 | ⭐⭐⭐ | ⭐⭐⭐⭐（Windows） |

**Linux用户推荐Code::Blocks**，Windows用户优先Dev-C++。

## 本章小结

1. Code::Blocks是跨平台的C++ IDE
2. 新建项目选Console Application，选C++
3. F9编译运行，F8开始调试
4. 调试时用Watches窗口查看变量值
5. Linux用户推荐Code::Blocks，Windows用户优先Dev-C++
""",

    "gpp-basics": """# g++编译基础

g++是Linux和竞赛环境中最常用的C++编译器。无论是Linux系统还是macOS，都自带g++。CSP-J/S复赛的评测环境也是基于g++的。掌握g++是每一位竞赛选手的必备技能。

![Linux终端——g++编译并运行C++程序的完整流程截图](/media/chapters/basics/embed_p05.png)

## 基本编译命令

### 最简单的编译

```bash
g++ -o program source.cpp
./program
```

- `source.cpp`是源文件
- `-o program`指定输出文件名
- `./program`运行程序

### 如果不加-o选项

```bash
g++ source.cpp   # 默认生成 a.out
./a.out           # 运行
```

## 常用编译选项

| 选项 | 说明 |
|------|------|
| `-o <file>` | 指定输出文件名 |
| `-Wall` | 显示所有警告（推荐常开） |
| `-g` | 包含调试信息（gdb用） |
| `-O2` | 二级优化（竞赛常用） |
| `-std=c++17` | 使用C++17标准 |
| `-static` | 静态链接，不依赖外部库 |
| `-s` | 去除符号表，减小文件大小 |
| `-lm` | 链接数学库 |

### 竞赛推荐编译命令

```bash
g++ -std=c++17 -O2 -pipe -static -s -Wall -o program source.cpp
```

解释：
- `-std=c++17`：C++17标准
- `-O2`：二级优化
- `-pipe`：用管道代替临时文件，加快编译
- `-static`：静态链接，程序不依赖外部库
- `-s`：去除调试信息，减小文件大小
- `-Wall`：显示所有警告

## 常见编译错误

### 未链接数学库

```bash
undefined reference to `sqrt'
```

**解决**：加`-lm`选项
```bash
g++ source.cpp -lm -o program
```

### 权限被拒绝

```bash
bash: ./program: Permission denied
```

**解决**：
```bash
chmod +x program    # 添加执行权限
./program            # 再运行
```

## 编译过程详解

```bash
# 预处理（展开头文件）
g++ -E source.cpp -o source.i

# 编译（生成汇编）
g++ -S source.i -o source.s

# 汇编（生成目标文件）
g++ -c source.s -o source.o

# 链接（生成可执行文件）
g++ source.o -o program
```

用`-v`选项可以查看完整编译过程：
```bash
g++ -v -o program source.cpp
```

## 本章小结

1. `g++ -o program source.cpp`是最基本的编译命令
2. 竞赛推荐：`-std=c++17 -O2 -Wall -static -s`
3. 链接数学库：加`-lm`选项
4. 遇到`Permission denied`：用`chmod +x`加执行权限
5. 用`-v`可以查看编译详细过程
""",

}


def main():
    db = SessionLocal()
    updated = 0
    for slug, content in CHAPTERS.items():
        chapter = db.query(Chapter).filter(Chapter.slug == slug).first()
        if not chapter:
            print(f"⚠️ 未找到：{slug}")
            continue
        chapter.content = content
        updated += 1
        print(f"✅ {chapter.order:02d}. {chapter.title}（{len(content)}字）")

    db.commit()
    print(f"\n共更新 {updated} 章")
    db.close()


if __name__ == "__main__":
    main()
